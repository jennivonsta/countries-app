// CountryDetail is a placeholder page for Version 0.
// This component will be expanded later when backend functionality is added.
function CountryDetail() {
  return (
    // <main> is a semantic HTML element used for the primary content of the page
    <main className="page">
      
      {/* Page heading */}
      <h1>Country Detail</h1>

      {/* 
        This message explains that the page is intentionally incomplete.
        The route exists so the app structure is ready for future work.
      */}
      <p>Version 0: This page is not required yet.</p>
    </main>
  );
}

// Export the component so it can be used in App.jsx routing
export default CountryDetail;
