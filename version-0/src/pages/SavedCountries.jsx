// SavedCountries is a placeholder page for Version 0.
// The route exists so the app structure matches the project requirements.
function SavedCountries() {
  return (
    // <main> represents the primary content area of the page
    <main className="page">
      
      {/* Page heading */}
      <h1>Saved Countries</h1>

      {/* 
        This message clarifies that this page is intentionally incomplete.
        It will be implemented later when backend functionality is introduced.
      */}
      <p>Version 0: This page is not required yet.</p>
    </main>
  );
}

// Export the component so it can be used in App.jsx routing
export default SavedCountries;
